export class Book {
    constructor(
        title: string,
        authors: string[],
        imagePath: string,
        avgRating: number,
        ratingCounts: number,
        publisher: string,
        description: string,
        language: string,
        categories: string[],
        pageCount: number
    ) { }
}